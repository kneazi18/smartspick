# Email Configuration (Required for contact form)
# Use Gmail SMTP with App Password (not regular password)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-16-digit-app-password

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://smartspicks.com

# Production Environment Variables
# These are automatically set by hosting providers like Vercel
VERCEL_URL=your-deployment-url.vercel.app

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the example values with your actual values
# 3. For Gmail: Enable 2FA and generate an App Password
# 4. Never commit .env.local to version control